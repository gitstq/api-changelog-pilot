# APIChangelog-Pilot - 依赖文件
# 零依赖设计，保持轻量化

# 核心依赖（可选）
# PyYAML - 用于YAML格式支持
# PyYAML>=5.4.0

# 开发依赖
pytest>=7.0.0
pytest-cov>=3.0.0

# 代码质量
flake8>=5.0.0
black>=22.0.0
mypy>=0.990

# 打包
build>=0.8.0
twine>=3.8.0
wheel>=0.38.0
